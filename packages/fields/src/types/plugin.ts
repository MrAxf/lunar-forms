export interface PluginOptions {
  theme: {
    classes: {
      outer: string;
      wrapper: string;
      label: string;
      inner: string;
      prefix: string;
      suffix: string;
      input: string;
      help: string;
      message: string;
    };
    icons: {
      placeholder: string;
    };
  };
  messages: {
    placeholder: string;
  };
}
